import { css } from "@emotion/core"

const name = css`
    font-size: 40px;
    margin-top: 20px;
`

const location = css`
    font-size: 18px;
`

const title = css`
    font-size: 25px;
    margin-top: 20px;
`

const experience = css`
    font-size: 18px;
    margin-top: 5px;
`

const bold = css`
    font-weight: 600;
`

export default {
    name,
    location,
    title,
    experience,
    bold,
}
